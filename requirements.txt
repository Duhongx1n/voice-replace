# 核心依赖
openai-whisper>=20231117
qwen-tts>=0.1.0
torch>=2.0.0
soundfile>=0.12.0
numpy>=1.24.0

# 可选依赖（提升参考音频质量）
demucs>=4.0.0

# 可选依赖（字幕去除 — 智能模式）
# paddlepaddle>=2.5.0
# paddleocr>=2.7.0
# opencv-python-headless>=4.8.0

# 可选依赖（大模型智能分句，支持 OpenAI 兼容 API）
# openai>=1.0.0

# edge-tts 备选引擎（在线 TTS，无需 GPU）
# edge-tts>=6.1.0
